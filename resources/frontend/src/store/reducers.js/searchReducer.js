const InitialState = [];

function searchReducer(state = InitialState, action) {
  switch (action.type) {
    case "q":
      break;

    default:
      return state;
  }
}

export default searchReducer;
